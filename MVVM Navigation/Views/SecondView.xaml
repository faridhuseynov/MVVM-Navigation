<UserControl x:Class="MVVM_Navigation.Views.SecondView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM_Navigation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Label FontSize="50">SECOND</Label>
        <TextBox FontSize="50" Text="{Binding Text,UpdateSourceTrigger=PropertyChanged}" ></TextBox>
        <Button FontSize="50" Command="{Binding SendCommand}">SEND TO FIRST</Button>
        <Button FontSize="50" Command="{Binding SendCommand}">SEND TO THIRD</Button>
    </StackPanel>
</UserControl>
